# Журнал: Lab 2: Motions in 2 and 3 Dimensions

Записи добавляются в конец и не переписываются задним числом.

| Дата | Стадия | Действие | Результат / артефакт | Решение пользователя |
|---|---:|---|---|---|
| 2026-09-09 | 0 | Создан каркас материала | `materials/labs/motions-in-2-and-3-dimensions/` | Ожидается |


## 2026-09-09 — Приёмка Lab 2

- Запрос пользователя: «Теперь добавь новую лабу: Lab 2 Motions in 2 and 3 dimensions»;
  после прерывания — «продолжи». Это разрешение создать каркас; подтверждения
  завершённой стадии или содержания пока нет.
- Выполнено: `make new TYPE=lab SLUG=motions-in-2-and-3-dimensions TITLE='Lab 2: Motions in 2 and 3 Dimensions'`.
- Обновлён `PLAN.md`: название, номер, формат, доступные данные и открытые вопросы.
- В `content.tex` оставлен только титульный каркас; удалены типовые учебные
  заполнители, формула и процедура из шаблона. Условия, данные и решения не добавлялись.
- `source/` пока содержит только `.gitkeep`; оригиналов преподавателя нет.
- Стадия 0: `awaiting_approval`; стадии 1–6: `pending`.
- Подготовлены обе точки входа с общим содержимым; результаты проверок будут
  добавлены отдельной записью.

## 2026-09-09 — Поступление исходников во время приёмки

После первоначального создания каркаса в `source/` обнаружены два PDF:

- `Lab 2 Motions in 2 and 3 dimensions.pdf`: 20 страниц, 960×540 pt, 1 057 417 байт.
- `Lab 2 Motions in 2 and 3 dimensions (short version).pdf`: 7 страниц, 960×540 pt, 763 697 байт.

Независимый агент проверил инвентаризацию по метаданным. Файлы не изменялись;
содержание задач не извлекалось. Текущий `PLAN.md` обновлён, титульная пометка
заменена на `Intake pending approval`. Предложено согласовать полную версию
как основной источник, короткую — для сверки; это пока не решение пользователя.
Контрольные суммы записаны в `output/materials/labs/motions-in-2-and-3-dimensions/source-inventory.json`.
Статус остаётся `awaiting_approval` на этапе 0.

Первоначальная сборка без расширенного доступа остановилась из-за недоступности
Docker socket. Повторный запуск с разрешённым доступом успешно собрал обе версии.
После уточнения титульной пометки нужна актуальная пересборка и визуальная проверка.

## 2026-09-09 — Проверка актуального каркаса

- `make lint` — успешно.
- Образ проверен через `docker image inspect`; сборка выполнена с digest
  `ghcr.io/slauva/master-thesis-2024-2026/latex@sha256:d123cc7a962ebc4f632cd217e8eaef39b5ab85deff01e85415241cc2717106c4`.
- Команды воспроизведения (из корня репозитория):

```bash
make lint
make build FILE=materials/labs/motions-in-2-and-3-dimensions/main.tex IMAGE=ghcr.io/slauva/master-thesis-2024-2026/latex@sha256:d123cc7a962ebc4f632cd217e8eaef39b5ab85deff01e85415241cc2717106c4
make build FILE=materials/labs/motions-in-2-and-3-dimensions/solution.tex IMAGE=ghcr.io/slauva/master-thesis-2024-2026/latex@sha256:d123cc7a962ebc4f632cd217e8eaef39b5ab85deff01e85415241cc2717106c4
```

- Артефакты: `output/materials/labs/motions-in-2-and-3-dimensions/main.pdf`
  и `solution.pdf`, по одному титульному слайду. Это каркасы, а не учебные колоды.
- `pdfinfo`: обе страницы 453.54×255.12 pt, отношение сторон 16:9.
- `pdftotext`: английский заголовок и пометки версий; кириллицы нет.
- Финальные LaTeX-журналы: warnings, overfull/underfull и missing glyphs не обнаружены.
- Обе страницы отрендерены через `pdftoppm -png -r 120` и просмотрены:
  обрезки, наложений и нечитаемых надписей нет. Различие версий видно в подзаголовке.
- Контрольные суммы обоих оригиналов повторно сверены — изменений нет.
- Независимый аудит каркаса подтвердил общие исходники двух версий,
  отсутствие выдуманного содержания и самовольного утверждения этапа.
- Этап 0 остаётся `awaiting_approval`; для следующего этапа требуется решение
  пользователя о границах и приоритете источников. Длительность и аудитория
  остаются открытыми вопросами для последующего планирования.

## 2026-09-09 — Уточнение требований и начало извлечения

Ответ пользователя на передачу этапа 0:

> Добавил исходники, нужен тот же стиль что и по первой лабе. Если есть несколько вариантов решения задачи можно его представить.

Этот ответ подтверждает работу с предоставленными исходниками и задаёт дальнейшие
требования к материалу. Приёмка считается подтверждённой в контексте предыдущего
запроса; этап 0 отмечен `approved`, этап 1 начат. Это не утверждение будущего
конспекта, плана слайдов, содержания или финального макета.

Зафиксировано:

- оформление как в текущей версии Lab 1, включая университетский титул,
  типографику, цвета, оформление условий и последовательные слайды решений;
- допустимы несколько содержательно разных способов решения, когда это полезно;
- разные решения помещаются только в версию с решениями, общие условия не дублируются;
- приоритет полной версии устанавливается по результатам сравнения полноты,
  а не приписывается отдельному утверждению пользователя.

Оба PDF извлечены через `pdftotext -layout` и отрендерены через `pdftoppm -png -r 72`.
Производные файлы находятся в `output/materials/labs/motions-in-2-and-3-dimensions/source-review/`.
Запущены независимое сравнение двух источников и проверка исходных формул и ответов.
Оригиналы в `source/` не редактируются.

## 2026-09-09 — Завершение этапа 1: конспект и сверка

- Создан `SOURCE_DIGEST.md`; обновлён `PLAN.md` с требованиями пользователя,
  соответствием источников, текущим состоянием и критериями следующего согласования.
- Проверены все 20 страниц F и 7 страниц S: текстовый слой извлечён через
  `pdftotext -layout`, страницы отрендерены через `pdftoppm -png -r 72`,
  просмотрены контактные листы и подробные изображения значимых страниц.
- Независимый агент сопоставил версии: S1→F4, S2→F5, S3→F7, S4→F9,
  S5→F12, S6→F15, S7→F18. Новых задач в S нет; S1 уже содержит решение,
  F2/F3 — точный дубль. Полная версия принята рабочим источником по полноте.
- Проверяющий физики независимо подтвердил ответы и выявил: неполное правило
  азимута, ошибочную геометрию A/B, неопределённость θ(0), неверную промежуточную
  подстановку постоянной компоненты скорости при движении по параболе,
  неоговорённые единицы и модельные предпосылки.
- Численные ответы к самолёту 13.6° E of N и 88.3 km/h найдены на изображении F8:
  они присутствуют в оригинале, хотя отсутствуют в его извлечённом тексте.
- Оба метода задачи 7 (минимизация времени и скорость сокращения оставшегося
  пути) — исходные. Дополнительные методы к задачам 1, 2 и 6 помечены как
  редакционные предложения. Для задачи 5 исключение времени названо
  альтернативной подачей исходного расчёта, а не отдельным новым методом.
- Сам конспект прошёл повторный независимый научный просмотр; внесены два
  уточнения: явно назвать отсутствие течения в задаче 7 и уточнить статус
  альтернативной подачи задачи 5. Остальных необходимых исправлений не найдено.
- Стиль сопоставлен с текущим `content.tex`, разделом `Current Stage 3 revision`
  в `SLIDE_PLAN.md` и `assets/README.md` Lab 1. Не использован архивный план
  с дополнительными roadmap/recap и выдуманной экспериментальной процедурой.
- `make lint` — успешно; `git diff --check` — успешно; ссылки и пробельные
  окончания новых Markdown проверены; SHA-256 обоих исходников неизменны.
- На этом шаге изменены только рабочие Markdown-файлы: LaTeX и PDF не
  пересобирались, они остаются ранее проверенными титульными каркасами.
- Этап 1: `awaiting_approval`. Этапы 2–6 не начинались. Пользователю передаётся
  конспект для проверки полноты, замечаний C01–C09 и границ дополнительных методов.

## 2026-09-09 — Утверждение этапа 1 и начало планирования

Пользователь ответил «дальше» на предложение утвердить конспект с замечаниями
и перейти к плану слайдов. Это однозначная команда перехода в данном контексте:
этап 1 отмечен `approved`, этап 2 начат без повторного запроса.

Для этапа 2 готовятся `SLIDE_PLAN.md`, последовательность общих и условных
слайдов, контракты рисунков, распределение альтернативных решений и критерии
приёмки. Учебный LaTeX и оригиналы не меняются. Оформление сверяется с текущей
Lab 1 независимым проверяющим; архивный план её ранней версии не применяется.

## 2026-09-09 — Завершение этапа 2: план слайдов

- Создан `SLIDE_PLAN.md`; актуализирован `PLAN.md`. Предложен объём:
  9 общих кадров + 17 кадров решений = 26 в версии с решениями.
- Сохранены семь задач и исходная справка; решения идут сразу после условий.
  Вторые способы предусмотрены для задач 1, 2, 6 и 7; оба метода задачи 7
  исходные, остальные дополнительные методы помечены по происхождению.
- Определены 13 контрактов схем, включая отдельные рисунки условий и решений,
  исправленную коллинеарность A/B, график θ(t), правильное направление ветра,
  кривизну параболы и геометрию быстрейшего пути.
- Независимый проверяющий стиля сверил текущий Lab 1: локальную обложку,
  происхождение кампуса/логотипа, общую тему, шрифты, поля, карточки ответов
  и чередование solutionframe. Специфические задачи и старый расширенный
  план Lab 1 не переносятся.
- Независимый проверяющий физики подтвердил соответствие утверждённому
  конспекту, покрытие условий и разделение общих/решённых схем. По замечаниям
  уточнены компенсация западного ветра, направление отсчёта θ и точная
  осцулирующая окружность с одинаковым масштабом осей.
- Проверены количество и уникальность ID: 9 shared, 17 solution, 13 схем;
  все семь задач имеют последовательные собственные решения; предлагаемые
  заголовки слайдов на английском.
- `make lint` и `git diff --check` — успешно. На этапе изменены только Markdown:
  сборка PDF не требовалась, LaTeX и оригиналы не менялись.
- Этап 2: `awaiting_approval`; этапы 3–6 не начинались. Следующий результат
  после утверждения — полный LaTeX-черновик двух версий в стиле Lab 1.

## 2026-09-09 — Утверждение этапа 2 и начало черновика

Пользователь ответил «дальше» на передачу плана и предложение перейти к полному
LaTeX-черновику. Этап 2 отмечен `approved`, этап 3 начат без повторного запроса.
Подтверждение относится к структуре 9/26, семи задачам, четырём вторым способам,
13 схемам и оформлению текущей Lab 1; оно не утверждает ещё не показанный черновик.

Работа разделена: два исполнителя создают непересекающиеся наборы TikZ G01–G07
и G08–G13, третий готовит фрагмент задач 6–7 в `output/`; основной агент собирает
общий `content.tex`, переносит титульные активы, выполняет сборки и ведёт журнал.
Титульные активы скопированы без изменения из Lab 1 с происхождением в
`assets/README.md`. Оригиналы Lab 2 не редактируются.

## 2026-09-09 — Этап 3: полный LaTeX-черновик и его проверка

### Результат

- Написан общий `content.tex`: титул в стиле текущей Lab 1, справка по
  координатам, семь исходных задач в прежнем порядке, 17 слайдов решений.
  `main.tex` и `solution.tex` используют соответственно `nosolutions` и
  `solutions` класса `physicslabslides` и подключают один общий текст.
- Созданы 13 векторных схем в `figures/` по G01–G13 из `SLIDE_PLAN.md`.
  На условиях используются отдельные схемы без найденных ответов.
- Перенесены без изменения фотография и логотип из Lab 1; происхождение
  отмечено в `assets/README.md`. Общие классы и тема не редактировались.
- Вторая методика есть у задач 1, 2, 6 и 7: общий луч, нормальная компонента
  скорости дождя, кривизна графика и предельное сокращение пути плавания.
  Учтены утверждённые замечания C01–C09 из `SOURCE_DIGEST.md`.
- Длинные заголовки P06, P07 и S06B сокращены до «Motion Along a Parabola»,
  «Walking and Swimming» и «Acceleration and Radius». Содержание и число
  кадров сохранены: 9 shared + 17 solution = 26. Заполнителей нет.

### Предварительная проверка физики

Независимый `physics_reviewer` сверил текст и все схемы с источниками,
включая растровые ответы F8 и график F9. Обязательных научных правок нет:

- задача 1: AB = 4 в неопределённых источником единицах длины;
- задача 2: одинаковые объёмы сбора S v_r T и одинаковая нормальная скорость;
- задача 3: 13.633° восточнее севера, 88.335 km/h относительно земли;
- задача 4: e ≈ 3.501 m/s, f ≈ −0.250074 m/s², направление при t = 0
  заменено правым пределом, отсчёты графика приближённые;
- задача 5: t_L = 1 s, y(L) ≈ 12.4205 m при явно указанном g = 9.8 m/s²;
- задача 6: дифференцирование до подстановки вершины, a = 2 c v₀² j,
  модуль 2 |c| v₀² и R = 1/(2 |c|), отдельно прямолинейный случай;
- задача 7: s* ≈ 186.5836 m, T'' > 0 и допустимость внутренней точки;
  оба способа совпадают. Независимая проверка времени: 180.481 s против
  369 s при плавании от A и 183.857 s при ходьбе до проекции C.

Сокращения текста при ремонте макета сохранили эти выводы и условия.
Это проверка черновика, а не утверждение следующей контрольной точки.

### Сборка и устранённые дефекты

Использован локально доступный Docker-образ с конкретным digest:

```sh
make lint
make build FILE=materials/labs/motions-in-2-and-3-dimensions/main.tex IMAGE=ghcr.io/slauva/master-thesis-2024-2026/latex@sha256:d123cc7a962ebc4f632cd217e8eaef39b5ab85deff01e85415241cc2717106c4
make build FILE=materials/labs/motions-in-2-and-3-dimensions/solution.tex IMAGE=ghcr.io/slauva/master-thesis-2024-2026/latex@sha256:d123cc7a962ebc4f632cd217e8eaef39b5ab85deff01e85415241cc2717106c4
```

Сборки выполнялись через разрешённый доступ к Docker socket; локальный TeX
не использовался. Консольные отчёты: `output/materials/labs/motions-in-2-and-3-dimensions/build-main.txt`
и `build-solution.txt` в том же каталоге.

В первых сборках обнаружены вертикальные переполнения из-за масштабирования
высоких схем по ширине колонки. Исправлены ограничения высоты, размещение
карточек, объединение связанных формул и длина пояснений. Устранён перенос
заголовка последней задачи. Промежуточная ошибка `Paragraph ended before
\answer was complete` исправлена заменой абзаца внутри короткого аргумента
карточки на `aligned`. На схеме G12 разнесены близкие подписи `water` и `v_g`.

Последние сборки обеих точек входа успешны. В итоговых `main.log` и
`solution.log` нет ошибок, предупреждений, missing glyphs, overfull/underfull
boxes. Общий стиль не менялся, поэтому `make check` не требовался.

### Проверка готовых PDF

- `output/materials/labs/motions-in-2-and-3-dimensions/main.pdf`: 9 страниц.
- `output/materials/labs/motions-in-2-and-3-dimensions/solution.pdf`: 26 страниц.
- Размер каждой страницы: 453.54 × 255.12 pt, отношение 16:9.
- Все страницы отрендерены Poppler при 150 dpi в `output/.../draft-review/`.
  Основной агент просмотрел все 9 студенческих страниц, независимый
  `latex_reviewer` — все 26 страниц решений, включая повторную проверку
  исправленных 9, 17 и 23–25. Открытых замечаний по макету нет: заголовки,
  формулы, рисунки, подписи и карточки читаемы, не обрезаны и не перекрываются.
- Проверено совпадение общих страниц по извлечённому тексту с исключением
  метки версии титула и счётчика внизу: student 1–9 соответствуют solution
  1, 2, 3, 7, 10, 13, 16, 19, 23. В каждой версии ровно семь условий.
- В student нет `SOLUTION VERSION`, `Answer:` и `Instructor note:`;
  в solution ровно 17 пометок решений. В тексте обеих PDF нет кириллицы.
- SHA-256 обоих исходных PDF повторно совпали с `SOURCE_DIGEST.md`.
- В `output/.../draft-review/verification.json` сохранены результаты
  проверки вариантов, числа страниц, размеров и контрольных сумм.
  Извлечённый текст, контактные листы и все временные материалы находятся
  только в `output/`.

### Передача

Этап 3 переведён в `awaiting_approval`. Для просмотра — обе PDF и общий
`content.tex`. Пользователь должен подтвердить полноту черновика и
готовность перейти к проверке содержания. Этапы 4–6 остаются `pending`.
Решение пользователя по черновику: **ожидается**.

## 2026-09-11 — Замечания пользователя к черновику, этап 3

Пользователь запросил восемь групп правок: пояснить atan2 и исправить
наложение подписей координатной схемы; удалить «Use the Common Ray» и
«Use the Normal Velocity»; вернуть схему с 45° и исходный вывод задачи 3,
явно выделить искомые угол и скорость; для задачи 4 использовать стрелки
векторов и шляпки базисов, c1/c2 вместо e/f, показать связь вектора с углом
и происхождение x(t), y(t), убрать единицы у коэффициента 5 внутри формул
и запись через пределы; в задаче 6 сразу дать локальные производные в
вершине, убрать второй способ и базисы в ответе; в задаче 7 сохранить
первый слайд решения, затем непосредственно выразить AB и прояснить второй
исходный способ.

Это доработка текущего черновика, а не утверждение перехода. Этап 3
возвращён из `awaiting_approval` в `in_progress`; этапы 4–6 не начинаются.
Исходные PDF и чужие файлы сохраняются. Основной агент меняет задачи 1–4
и общие журналы, исполнитель — только участок задач 6–7 и их схему;
независимый проверяющий сверяет спорные страницы F7–F11.

## 2026-09-11 — Этап 3: исправленный черновик по восьми замечаниям

### Изменения

- `content.tex`: объяснён двухаргументный atan2 с примером выбора квадранта;
  убраны S01C «Use the Common Ray» и S02B «Use the Normal Velocity».
- Задача 3: явно перечислены θ и v_PE; восстановлен треугольник F7 с 45°
  и исходный порядок F8: Пифагор для скорости, затем синус для угла.
- Задача 4: c1/c2 вместо e/f, стрелки векторов и шляпки базисов; координаты
  явно получены из коэффициентов вектора. Добавлена схема связи r,x,y,θ.
  Решение возвращено к двум отсчётам графика, число 5 без единиц внутри
  промежуточных формул. Пределы исключены, но сохранено краткое корректное
  пояснение начального направления и нулевого вектора.
- Задача 6: четыре локальные производные заданы в условии только при x=0,
  c>0 явно соответствует исходному рисунку. Один слайд решения вместо трёх;
  a=2cv0² вверх и R=1/(2c), без базисов и второго метода.
- Задача 7: текст первого слайда решения сохранён побайтно. Второй только
  выражает AB; третий поясняет исходное равенство скоростей сокращения BC
  и плавания через проекцию v_g cos α, без ds и дополнительных производных.
- Схемы: исправлены `spherical-coordinate-convention.tikz`,
  `wind-velocity-triangle.tikz`, `direction-angle-check.tikz`,
  `route-shortening.tikz`, `walking-swimming-route.tikz`; добавлена
  `position-vector-angle.tikz`. Всего 14 используемых схем.
- `PLAN.md` и `SLIDE_PLAN.md` актуализированы. Общие классы, тема
  и исходные PDF не менялись.

### Контракты изменённых рисунков

- G01: прежние оси и сферическая конвенция; подписи O и φ разведены.
- G05: исходный порядок O→P (v_PA), W→O (v_AE), W→P (v_PE пунктиром),
  θ от North к воздушной скорости, 45° между East и лучом против ветра.
  Одинаковый масштаб осей, ветер к юго-западу, ответов на условии нет.
- G07: прежняя проверочная кривая, подпись начального угла без θ(0+)
  и записи через пределы; приблизительные отсчёты 35° и 14 s.
- G12: прежняя геометрия маршрута; C смещена влево от выносной линии H.
- G13: B,C, скорость вдоль берега и её проекция на BC; правильный угол α,
  без B′/ds; рисунок явно схематический.
- G14: общий радиус-вектор в первом квадранте, x вправо, y вверх, θ от +x,
  прямоугольные проекции и базисы со шляпками. Численные данные не добавлены.

### Проверка физики

Независимый physics_reviewer сверил F7–F11, F15–F20 и итоговые P3/P4/P6/P7.
Обязательных замечаний нет: v_PE=88.3348 km/h, θ=13.6330°; c1≈3.5 m/s,
 c2≈−0.25 m/s²; a=2cv0², R=1/(2c) при явно указанном c>0;
AB=186.5836 m≈187 m. Единственность минимума задачи 7 проверена отдельно
от слайдов. Неизменённые задачи повторной расширенной проверке не подвергались.

### Сборки и макет

```sh
make lint
make build FILE=materials/labs/motions-in-2-and-3-dimensions/main.tex IMAGE=ghcr.io/slauva/master-thesis-2024-2026/latex@sha256:d123cc7a962ebc4f632cd217e8eaef39b5ab85deff01e85415241cc2717106c4
make build FILE=materials/labs/motions-in-2-and-3-dimensions/solution.tex IMAGE=ghcr.io/slauva/master-thesis-2024-2026/latex@sha256:d123cc7a962ebc4f632cd217e8eaef39b5ab85deff01e85415241cc2717106c4
```

Первая sandbox-сборка не получила доступ к Docker socket; затем сборки
выполнены с разрешённым доступом к тому же закреплённому Docker-образу.
Исправлены недопустимый абзац внутри answer, переполнение S06A, слишком
длинный заголовок S07C и слабая строка набора S07C. В окончательных логах
нет Overfull/Underfull, предупреждений о шрифтах, глифах, ссылках или ошибок.

Результат: `main.pdf` — 9 страниц, `solution.pdf` — 22 страницы;
453.54×255.12 pt, 16:9. Основной агент и latex_reviewer визуально просмотрели
все 31 страницы, повторно проверили изменённые после ремонта. Обрезки,
наложений, непонятных направлений и пропавших символов нет.

Контроль версий: main 1–9 соответствуют solution 1,2,3,6,8,11,14,17,19.
Их текст совпадает за исключением названия версии и счётчика; растры общих
кадров 2–9 совпадают вне колонтитула. В student нет решений; в solution
13 solutionframe. Удалённые названия отсутствуют, кириллицы нет.
SHA-256 исходных PDF совпадают с сохранёнными до правки.

Журналы сборок, извлечённый текст, растры и `verification.json` находятся
в `output/materials/labs/motions-in-2-and-3-dimensions/revision-review/`.
`make lint`, проверка ссылок изменённых Markdown и пробельных окончаний
пройдены. Изменений вне материала и его output нет.

Исправленный этап 3 возвращён в `awaiting_approval`. Пользовательская
приёмка доработки ожидается; переход к этапам 4–6 не выполнялся.

## 2026-09-11 — Этап 3: формула atan2, схема в условии и короткая стрелка

Пользователь уточнил три требования: нужна формула вычисления atan2,
схема связи угла с радиус-вектором должна стоять в условии задачи 4,
а в задаче 7 вместо двойной стрелки требуется простая `	o`.
Это правки текущего черновика; статус `awaiting_approval` сохранён,
переход к следующим этапам не выполнялся.

### Изменения

- В `content.tex` добавлен общий справочный слайд C03 с пятью ветвями
  atan2(y,x): знаки x/y, значения на обеих полуосях y, главная ветвь arctan,
  неопределённость при (0,0) и перевод отрицательных углов в [0,2π).
  Предыдущее краткое описание и пример сохранены. Формула вынесена
  на отдельный слайд для читаемого набора: теперь 10/23 слайда.
- В P04 перенесена существующая `position-vector-angle.tikz`; рядом
  остаётся исходный график θ(t). Контракт G14 и сама геометрия не меняются:
  x вправо, y вверх, θ от +x, общий r и координатные проекции. Размер
  на условии — 0.36 высоты страницы; исходный график — 0.46 высоты.
  В решении S04A схема удалена, текст и формулы занимают ширину слайда.
- В S07A заменена только `\Longrightarrow` на `	o`, то есть одна
  обычная стрелка перехода; остальной вывод сохранён.
- Актуализированы `PLAN.md`, `SLIDE_PLAN.md`: число кадров, C03,
  расположение G14 и критерии текущей правки. Исходники и общая тема
  не менялись; файлы TikZ не редактировались.

### Проверки

Независимый physics_reviewer подтвердил все пять ветвей atan2, значения
на осях, пример atan2(1,−1)=135° и перевод диапазона. Проверка не
расширялась на неизменённые решения.

```sh
make lint
make build FILE=materials/labs/motions-in-2-and-3-dimensions/main.tex IMAGE=ghcr.io/slauva/master-thesis-2024-2026/latex@sha256:d123cc7a962ebc4f632cd217e8eaef39b5ab85deff01e85415241cc2717106c4
make build FILE=materials/labs/motions-in-2-and-3-dimensions/solution.tex IMAGE=ghcr.io/slauva/master-thesis-2024-2026/latex@sha256:d123cc7a962ebc4f632cd217e8eaef39b5ab85deff01e85415241cc2717106c4
```

Обе сборки через разрешённый доступ к Docker успешны, предупреждений нет.
Схемы условия P04 увеличены после первого просмотра для читаемости подписей;
после этого обе версии пересобраны и повторно просмотрены.
Основной агент и latex_reviewer проверили main 3,7 и solution 3,12,13,21:
обрезки и наложений нет. Остальные страницы сравнены по растрам с ранее
проверенным выпуском: тела страниц совпали без изменённого колонтитула.

`main.pdf`: 10 страниц, `solution.pdf`: 23, обе 16:9. Общие кадры main 1–10
соответствуют solution 1,2,3,4,7,9,12,15,18,20: текст совпал без метки
версии и нумерации, растры общих кадров 2–10 совпали вне колонтитула.
Кириллицы нет, студенческая версия не содержит решений, в преподавательской
13 solutionframe. Отдельно проверены перенос схемы только в условие P4
и отсутствие двойных стрелок в P7. SHA-256 исходных PDF не изменились.

Все текущие логи, растры, извлечённый текст и `verification.json` находятся
в `output/materials/labs/motions-in-2-and-3-dimensions/revision-2-review/`.
`make lint`, ссылки изменённых Markdown, число кадров, включения рисунков
и пробельные окончания проверены успешно. Этап 3 остаётся `awaiting_approval`.
